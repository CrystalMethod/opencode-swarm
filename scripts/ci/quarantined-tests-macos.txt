# macOS-only quarantined tests — CI's unit job skips these on macos-latest
# runners specifically (they pass on ubuntu-latest).
#
# Format: one repo-relative test file path per line. Blank lines and lines
# starting with # are ignored.
#
# METADATA REQUIREMENT (issue #2477): every active path entry must carry,
# in the comment block directly above it:
#   # OWNER: <owner> — <issue ref / context>
#   # EXPIRY: YYYY-MM-DD — <retirement criterion>
# Enforced by `bun run check:invariants` Check 7: missing metadata fails
# the gate; an EXPIRY more than 14 days in the past also fails (inside
# the grace window it only warns).
#
# Active macOS-only quarantine entries are below; each carries the Check
# 7-enforced OWNER/EXPIRY metadata.

# OWNER: zaxbysauce — issue #2738 (deterministic macOS signal-child failure:
#   the crash fixture survives the delivered POSIX signal and exits 0 on
#   macos-latest runners; 9/9 failures across PRs #2735/#2736 on 2026-09-12,
#   ubuntu shards green; test already win32-skipped in-source)
# EXPIRY: 2026-11-18 — retire when the #2738 signal-fixture root fix (SIGKILL
#   delivery or survival detection) lands and the file passes on macos-latest.
# Renewed 2026-09-25 per issue #2900 (Workstream I3); previous EXPIRY 2026-09-26.
tests/unit/scripts/ci/repository-validation-real-process-2675.test.ts
#
# tests/unit/utils/bun-compat-exit-first-2530.test.ts (issue #2740)
# ---------------------------------------------------------------------------
# Regression coverage for #2530: verifies the bunSpawn Node fallback consumes
# stdout/stderr exit-first (empty/small/large/nonzero dual-stream matrix,
# bounded-output overflow, timeout termination) across both native Bun and the
# plain-Node fallback, plus the public isCleanWorktree path under node
# --input-type=module. Every non-matrix test shell outs to real processes:
# `bun build` of src/utils/bun-compat.ts and a `node` probe, both under hard
# 5s timeouts (PROBE_TIMEOUT_MS at tests/unit/utils/bun-compat-exit-first-2530.test.ts:9).
# A macos-latest merge-group runner stalled by >5s on cold build/install or
# CPU contention trips the probe timeout, which surfaces as attempt-1 failure
# with no product defect involved. Pre-existing, unrelated to any specific PR.
# Quarantined per issue #2740; tracked under #1782 (test-stability sprint).
# OWNER: zaxbysauce — issue #2973 (I3 renewal-cohort retirement tracker;
#   original flake evidence: closed #2740, block history below)
# EXPIRY: 2026-11-18 — retire when the probe-timeout sensitivity is root-fixed
#   (raise PROBE_TIMEOUT_MS or pre-warm the bun build) and the file passes on
#   macos-latest merge-group runs (#2973 cohort row 14).
# Renewed 2026-09-25 per issue #2900 (Workstream I3); previous EXPIRY 2026-10-14.
tests/unit/utils/bun-compat-exit-first-2530.test.ts
#
# tests/unit/commands/promote-registration.test.ts (issue #2740)
# ---------------------------------------------------------------------------
# Unit tests for the `/swarm promote` command registration and behavior
# (command import/export, switch-case registration, help text, direct-text
# mode, --category/--from-swarm/--force/--reason parsing, and the #1821 A3
# actionability-floor gate). beforeEach mutates process.env.HOME /
# LOCALAPPDATA / XDG_DATA_HOME to redirect the hive knowledge path into a
# fresh mkdtempSync temp dir, and afterEach restores it and rmSync's the dir —
# the classic env-mutation + temp-dir teardown surface (#1782 class) plus a
# real filesystem write to the hive path per test. Attempt-1 failure on
# macos-latest merge-group runner with immediate retry pass is consistent
# with cold-file / env-read sensitivity, not a product regression.
# Pre-existing, unrelated to any specific PR.
# Quarantined per issue #2740; tracked under #1782 (test-stability sprint).
# OWNER: zaxbysauce — issue #2973 (I3 renewal-cohort retirement tracker;
#   original flake evidence: closed #2740, block history below)
# EXPIRY: 2026-11-18 — retire when the per-test env-mutation / temp-dir
#   teardown flake is root-fixed and the file passes on macos-latest
#   merge-group runs (#2973 cohort row 15).
# Renewed 2026-09-25 per issue #2900 (Workstream I3); previous EXPIRY 2026-10-14.
tests/unit/commands/promote-registration.test.ts
#
# tests/unit/hooks/pr-feedback-scope-controller.test.ts (issue #2730)
# ---------------------------------------------------------------------------
# Tests the PR_FEEDBACK dedicated coder scope controller (8 cases): fails
# closed before immutable verification settles, authorizes exactly one
# planless coder Task and enforces its bound files, gives an authenticated
# synthetic PR-feedback scope precedence over an unrelated plan, gives an
# authenticated PR-feedback scope precedence over stale plan Stage-A state,
# invalidates a prepared scope when the feedback revision changes,
# atomically reserves one PR-feedback declaration when Task calls race,
# does not consume a replacement declaration after classifying its
# predecessor, and preserves normal-plan Stage-A rejection without scope
# or evidence leaks. Flaky on macos-latest merge-group unit-shard 1 only:
# CI run 34670762927 (merge_group, head 1499a74b, 2026-09-12T03:56:04Z)
# had Attempt 1 fail and Passed on retry 1 (passed-on-retry flake); sibling
# macos-latest unit shards 2/4/5/6, all ubuntu-latest unit shards 1-4, and
# all windows-latest unit shards either ran the file green or did not run
# it (shard round-robin). The ci.yml retry loop discards attempt-1 output
# when a retry passes, so no assertion text exists to drive a root-cause
# fix; the file passes locally (8/8 cases). Pre-existing, unrelated to any
# specific PR. This is the last remaining issue #2730 candidate without an
# owning entry: init-rehome.test.ts is quarantined in the general ledger
# via issue #2660 (PR #2799), bun-compat-exit-first-2530.test.ts in this
# ledger via issue #2740 (PR #2769), and repository-validation-real-
# process-2675.test.ts in this ledger via issue #2738 — so this PR adds
# only this entry and pins the no-duplicate invariant for the other three.
# Quarantined per issue #2730; tracked under #1782 (test-stability sprint).
# OWNER: zaxbysauce — issue #2973 (I3 renewal-cohort retirement tracker;
#   original flake evidence: closed #2730, block history below)
# EXPIRY: 2026-11-18 — retire when the macos-only retry-flake has a
#   reproducible diagnosis or a root fix and the file passes on macos-latest
#   merge-group runs (#2973 cohort row 16).
# Renewed 2026-09-25 per issue #2900 (Workstream I3); previous EXPIRY 2026-10-14.
tests/unit/hooks/pr-feedback-scope-controller.test.ts
