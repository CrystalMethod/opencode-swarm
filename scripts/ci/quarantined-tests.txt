# Quarantined tests - CI's unit/coverage jobs skip these files.
#
# Format: one repo-relative test file path per line. Blank lines and lines
# starting with # are ignored.
#
# METADATA REQUIREMENT (issue #2477): every active path entry must carry,
# in the comment block directly above it:
#   # OWNER: <owner> — <issue ref / context>
#   # EXPIRY: YYYY-MM-DD — <retirement criterion>
# Enforced by `bun run check:invariants` Check 7: missing metadata fails
# the gate; an EXPIRY more than 14 days in the past also fails (inside
# the grace window it only warns).
#
# Active global quarantine entries are below; each carries the Check
# 7-enforced OWNER/EXPIRY metadata. (Issue #1908 retired the tracked backlog.)
#
# tests/unit/services/evidence-summary-adversarial.test.ts (issue #2740)
# ---------------------------------------------------------------------------
# Adversarial security tests for the Evidence Summary Pipeline (Task 5.8):
# malformed evidence payloads, blocker spoofing, artifact path abuse, and
# event spam — exercised through buildEvidenceSummary /
# createEvidenceSummaryIntegration with mocked loadEvidence / loadPlanJsonOnly.
# Core timing hazard: the "should not block on slow event handlers" case
# registers a real 100ms setTimeout handler and asserts
# expect(duration).toBeLessThan(500) at
# tests/unit/services/evidence-summary-adversarial.test.ts:882 — under
# merge-group runner CPU contention (6 unit shards co-scheduled with coverage
# and smoke jobs) the publish can exceed the 500ms budget and fail attempt 1,
# then pass on retry when the runner has settled. The temp dir per test is
# also mkdtempSync/rmSync'd. Pre-existing, unrelated to any specific PR.
# Quarantined per issue #2740; tracked under #1782 (test-stability sprint).
# OWNER: zaxbysauce — issue #2740 (ubuntu-latest merge-group retry-flake,
#   run 34726206593 `unit (ubuntu-latest, 2)`; attempts 1+2 failed, passed on
#   retry 2; no per-OS ledger exists for ubuntu so this uses the general one,
#   matching the #2368 dispatch-lanes precedent)
# EXPIRY: 2026-10-14 — root-fix the 500ms wall-clock budget (e.g. freeze the
#   clock or widen the margin) or renew with an updated criterion. After
#   EXPIRY, Check 7 warns for 14 days, then hard-fails on day 15.
tests/unit/services/evidence-summary-adversarial.test.ts

# tests/unit/telemetry/init-rehome.test.ts (issue #2660)
# ---------------------------------------------------------------------------
# Tests the telemetry re-home on directory change path (issue #2472 W9):
# init A then init B produces two telemetry.jsonl files and B receives
# subsequent events; same-directory re-init stays an idempotent no-op;
# events emitted before any init are dropped without creating a file;
# failed new-stream creation retains the previous project's stream (fail-open
# ownership retention); windows case-variant spelling of the same root keeps
# the stream destination (canonical same-directory check). Flake observed on
# ubuntu-latest merge-group coverage-shard 1: CI run 34309871068
# (merge_group, head e2491c3a729ea6fce5672f228a021e9dada6ebde,
# 2026-09-09T04:09:35Z) had Attempt 1 fail and Passed on retry 1
# (passed-on-retry flake); sibling coverage-shards 2/3/4/5/6 were green on
# this file in the same run, and all six ubuntu/macos/windows unit shards
# were green or did not run the file. The coverage job runs ubuntu-only
# and honors ONLY the general ledger (scripts/ci/run-coverage-gate.sh
# never branches per-OS), so this is the only ledger that reaches the
# coverage gate. The unit job also consumes this general ledger
# unconditionally on every RUNNER_OS (ci.yml:628 base grep, with the
# per-OS ledgers additive at ci.yml:632-636), so this entry skips the
# file in all unit cells and coverage shards — accepted over-provision
# because coverage honors only this ledger. The unit job's retry loop
# ("Run unit tests" step, ci.yml:696-721) reuses one output file across
# attempts, so attempt-1 output is discarded when a retry passes and no
# assertion text exists to drive a root-cause fix. Pre-existing, unrelated
# to any specific PR.
# Quarantined per issue #2660; tracked under #1782 (test-stability sprint).
# OWNER: zaxbysauce — issue #2660 (ubuntu-only merge-group coverage-shard
#   flake; general-ledger target because run-coverage-gate.sh never
#   branches per-OS)
# EXPIRY: 2026-10-15 — root-fix the coverage/isolate retry-flake or renew
#   with an updated criterion. Check 7 is silent until EXPIRY passes,
#   then warns for 14 days and hard-fails on day 15.
tests/unit/telemetry/init-rehome.test.ts
#
# pr-workflow-gate-batch-gc.test.ts (issue #2761) — CORE-TREE
# -----------------------------------------------------------------------------
# CORE-TREE entry requiring human review per the #1782 rule C: tests under
# tests/unit/hooks/** are deliberately not auto-suggested; this comment block
# documents why quarantine is appropriate. Tests the PR-workflow gate batch
# garbage collection introduced by issue #1968 P4 (MAX_WORKFLOW_BATCHES GC
# reclaim that fails closed when the derived inventory cannot be proven
# unchanged). Flaky by construction: the file header (lines 35-42 of the test
# itself) declares "every test here builds a full cap's worth of batches, which
# is inherently slow: ~4s against bun's 5000ms default, i.e. flaky by
# construction, and a timeout mid-transaction poisons the *next* test (its
# continuation writes into a temp directory teardown already replaced)" — the
# tests deliberately set `CAP_TEST_TIMEOUT_MS = 60_000` per-test to give
# themselves headroom against the same cold-FS pressure that has hit prior
# merge-group runs. Merge-group run 34797415390 (pr-2753, head
# 15dcd7f1731c9e380bf7fbddb95252bca095cf6d, 2026-09-14T01:54Z, flake-detection
# run 34799399563 at 02:29:49Z filed #2761): the file's
# flake-annotations-unit-shard-1 artifact (id 10331335105) carries the
# ::error file=...::FAILED annotation, meaning all three in-job retries
# (max_retries=2) timed out under merge-group pressure. The 6-test suite runs
# to 24.7s locally with explicit 60s timeouts (longest cell:
# "the validation batch cap is reclaimed in a reviewer retry loop" at
# 10915.95ms) and 6/6 pass locally on a clean TMPDIR — so the failure is
# timing / CPU-contention sensitive, not a logic bug. Per-OS attribution is
# indirect: the artifact's originating job log is no longer queryable from
# the actions API, but the cross-OS flake profile (no Windows or macOS-only
# signal — multiple ubuntu-latest shards also fail under cold-FS latency in
# the same merge-group matrix; see the historical #1729 / #1737 cross-OS
# pattern) routes this to the general ledger rather than
# quarantined-tests-{macos,windows}.txt. Pre-existing, unrelated to any
# specific PR. Quarantined per issue #2761; tracked under #1737 (test
# quarantine debt) pending a dedicated test-stability sprint (#1782).
# OWNER: @zaxbysauce — issue #2761 (CORE-TREE entry: tests/unit/hooks/** per
#   rule C of scripts/ci/detect-and-quarantine-flakes.sh; flaky-by-construction
#   per the file's own header comment; cross-OS timing sensitivity routes to
#   general ledger; see merge-group run 34797415390 + flake-annotations
#   artifact 10331335105)
# EXPIRY: 2026-10-16 — either (a) shorten the suite's per-test work
#   (e.g. shrink MAX_WORKFLOW_BATCHES for these tests via test-only override,
#   or pre-seed minimal fixtures so the full-cap loop is unnecessary), or
#   (b) raise the per-test timeout floor used by merge_group windows-latest /
#   ubuntu-latest cold-FS shards from 60s to 120s; root-fix the test's
#   inherent slowness, or renew with an updated diagnosis under
#   #1737/#2477
tests/unit/hooks/pr-workflow-gate-batch-gc.test.ts
